backend = "glx";
vsync = true;


blur : {
method = "gaussian";
size = 20;
deviation = 15;
};

blur-background-exclude = [
  "window_type = 'desktop'",
  "class_g = 'Firefox'",
];

fading = true;
fade-delta = 4;
